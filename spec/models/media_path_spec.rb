require 'spec_helper'

describe MediaPath do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    MediaPath.create!(@valid_attributes)
  end
end
